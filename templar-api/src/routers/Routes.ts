export enum UsersRoutes {
    GET = '/api/users',
    POST = '/api/users',
    PATCH = '/api/users/:id',
    DELETE = '/api/users/:id'
}

export enum AdminsRoutes {
    GET = '/api/admins',
    POST = '/api/admins',
    PATCH = '/api/admins/:id',
    DELETE = '/api/admins/:id'
}

export enum TokenRoutes {
    POST = '/auth/token'
}
